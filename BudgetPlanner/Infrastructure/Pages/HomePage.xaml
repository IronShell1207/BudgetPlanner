<Page
    x:Class="BudgetPlanner.Infrastructure.Pages.HomePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:BudgetPlanner.Infrastructure.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:BudgetPlanner.Infrastructure.Controls"
    mc:Ignorable="d"
    Foreground="Black"
    FontFamily="Segoe UI"
    Background="White">

    <Grid >
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="200"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <controls:AccountInfo Grid.ColumnSpan="3"
                              UserName="{x:Bind ViewModel.AppName}"
                              >

        </controls:AccountInfo>
        <controls:Operations Grid.Row="1" Grid.Column="2" Width="500"/>
    </Grid>
</Page>
